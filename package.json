{
  "name": "leetbox",
  "private": true,
  "workspaces": [
    "server"
  ],
  "devDependencies": {
    "prettier": "^3.9.6"
  },
  "scripts": {
    "compose": "docker compose",
    "up": "npm run compose -- up -d --wait",
    "down": "npm run compose -- down",
    "nuke": "npm run compose -- down -v",
    "db:migrate": "npm run db:migrate -w server",
    "db:seed": "npm run seed -w server",
    "preinit": "cp -n .env.example .env; true",
    "init": "npm run up && npm install && npm run db:migrate && npm run db:seed",
    "reset": "npm run nuke && npm run init",
    "dev": "npm run dev -w server"
  }
}
