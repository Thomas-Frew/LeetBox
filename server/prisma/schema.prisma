
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Question {
  id          Int            @id
  title       String         @unique
  titleSlug   String         @unique
  difficulty  String
}